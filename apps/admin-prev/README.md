# Ruto Admin SPA

## Run

```bash
pnpm install
pnpm dev
```

By default the API target is:

```text
http://localhost:80
```

Override via `.env`:

```text
VITE_API_BASE_URL=http://localhost:80
VITE_PORT=5174
VITE_HOST=127.0.0.1
```

## Build

```bash
pnpm build
pnpm preview
```
