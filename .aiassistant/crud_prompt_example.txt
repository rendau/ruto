Используй skill `crud`. Сделай полный CRUD для сущности `node`. Строго следуй правилам скилла (обязательно).

Поля:
- `id` string (PK)
- `name` string
- `ord` int

Таблица в БД: `node`

Требования:
1. Не упрощай и не изобретай свою структуру.
2. Повтори архитектурный паттерн 1-в-1
3. flow запуска приложение `make generate-proto` -> gofmt -> `go test ./...` -> go run ./cmd/.
